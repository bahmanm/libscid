# libscid-spelling

`libscid-spelling` provides spelling/name normalization support for database-oriented workflows. It depends on `LibScidCpp::Database`, so it sits above the core/database split rather than inside either foundational library.
