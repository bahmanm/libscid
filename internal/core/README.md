# libscid-core

`libscid-core` is the database-free chess domain library: board primitives, positions, moves, game/movetext, notation, PGN, dates, ratings, results and related value types. It has no dependency on `libscid-database` or application code.
